var config = {};

config.nuve_host = '';
config.service = {};
config.service.id ='';
config.service.key = '';

config.demo_host = ''; // Without "http", just the host.
config.https = false;

//config.plain = true;

module.exports = config;